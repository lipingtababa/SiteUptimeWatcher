name: Build 
run-name: ${{ github.actor }} is building ${{ github.repository }}
permissions:
    id-token: write
    contents: read
env:
    AWS_REGION: us-east-1
    IMAGE_TAG: ${{ github.sha }}
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get repo
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t watcher:${{ env.IMAGE_TAG }} .

      - name: Run pytest
        run: docker run watcher:${{ env.IMAGE_TAG }} pytest ./test -v

      - name: Run pylint
        run: docker run watcher:${{ env.IMAGE_TAG }} pylint ./src

      - name: Configure aws credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::339713007259:role/github-action
          role-session-name: github
          aws-region: ${{ env.AWS_REGION }}
      - name: Push Docker image
        run: |
            aws ecr get-login-password --region ${{env.AWS_REGION }} | docker login --username AWS --password-stdin 339713007259.dkr.ecr.us-east-1.amazonaws.com
            docker tag watcher:${{ github.sha }} 339713007259.dkr.ecr.us-east-1.amazonaws.com/watcher:${{ github.sha }}
            docker tag watcher:${{ github.sha }} 339713007259.dkr.ecr.us-east-1.amazonaws.com/watcher:latest
            docker push 339713007259.dkr.ecr.us-east-1.amazonaws.com/watcher:${{ github.sha }}
            docker push 339713007259.dkr.ecr.us-east-1.amazonaws.com/watcher:latest
      - name: Push secrets
        run: |
            aws secretsmanager put-secret-value --secret-id "/watcher/postgre/password" --secret-string ${{ secrets.DB_PASSWORD }} --region ${{env.AWS_REGION }}
  
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Get repo
        run: echo "The ${{ github.repository }} repository has been cloned to the runner."
